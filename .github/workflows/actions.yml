on:
  workflow_dispatch:

# jobs:
#   hybrid-job:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Run a command in ubuntu VM
#         run: echo "This runs on the Ubuntu VM"

#       - name: Run inside an Alpine container
#         uses: docker://alpine:3.24 # The image must be accessible without authentication
#         with:
#           entrypoint: /bin/sh
#           args: |
#             -c "echo 'This runs inside an alpine Container'"

#       - name: Run another command in Ubuntu VM
#         run: echo "Back to the VM"

#     dockerized_job:
#       runs-on: ubuntu-latest
#       container:
#         image: ghcr.io/my-org/my-private-image:latest
#         credentials:
#           username: ${{ secrets.DOCKER_USERNAME }}
#           password: ${{ secrets.DOCKER_PASSWORD }}
#       steps:
#         - run: node --version
#         - run: echo "This whole job runs inside a Node.js container "
jobs:
  job_1:
    strategy:
      fail-fast: false
      max-parallel: 3
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
        node_version: [14.x, 15.x, 16.x]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/setup-node@v3.6.0
        with:
          node-version: ${{ matrix.node_version }}